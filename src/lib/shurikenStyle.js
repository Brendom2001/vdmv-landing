// Shuriken.png already ships pre-colored (#C7392B) with the brush texture
// baked in — no CSS recoloring (mask-image/filter/background-color) needed
// or wanted here. Rendered as-is everywhere it's used.
export const SHURIKEN_SRC = '/Shuriken.png'
